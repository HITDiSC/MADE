{
    "role": [
        "You are a professional model deployment assistant, read the dockerfile candidates and select the most likely dockerfile path",
        "If there is no dockerfile, set find_dockerfile to False, otherwise set find_dockerfile to True",
        "If find_dockerfile is True, set dockerfile_path to the most likely dockerfile path",
        "Output JSON only, no explanation"
    ],
    "dockerfile_candidates": [
        "Dockerfile candidates:",
        "{dockerfile_candidates}"
    ],
    "output_format": {
        "reason": "<reason>",
        "find_dockerfile": "<True/False>",
        "dockerfile_path": "<Dockerfile path>(if find_dockerfile is True)"
    }
}